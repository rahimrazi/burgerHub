<section>
    <div class="container"> 
        <div class="">
            <a href="/admin/add-category" class="btn btn-success mt-5">Add Category</a>
        </div>
        <table class="table" id="category">
            <thead>
                <tr>
                    
                    <th scope="col">Number</th> 
                    <th scope="col">Category</th>
                    {{!-- <th scope="col">Discription</th>
                    <th scope="col">Price</th>
                    <th scope="col">Image</th> --}}
                    <th scope="col">Option</th>
                </tr>
            </thead>
            <tbody>
                {{#each categoryDetails}}
                <tr>
                    <td>{{inc @index}} </td>
                    <td>{{this.category}} </td>
                    {{!-- <td>{{this.description}} </td>
                    <td>₹{{this.price}}</td>
                    <td><img src="/product-images/{{this._id}}.jpg" style="width: 50px; height: 50px;"></td>
                    <td>
                        

                        
                        <a href="/admin/edit-product/{{this._id}}" class="btn btn-primary">Edit</a> --}}
                        <td>
                    <a href="/admin/delete-category/{{this._id}}" class="btn btn-danger" onclick="return confirm('Are  you want to delete {{this.Name}} ?')">Delete</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

</section>
<script>
    $(document).ready(function () {


        $('#category').wrap('<div id="hide" style="display:none"/>');

        $('#hide').css('display', 'block');
    });


    var table = $('#category').DataTable({"columnDefs": [
    { "width": "8%", "targets": 0 }
  ]
} );
</script>


